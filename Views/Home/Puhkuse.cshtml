@model IEnumerable<kutseAppEvtina.Models.Holidays>

@{
    ViewBag.Title = "Puhkuse";
}

<h2>Puhkuse</h2>

<p>
    @Html.ActionLink("Loo uus", "CreateH")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Muuda", "EditH", new { id=item.Id }) |
            @Html.ActionLink("Kustuta", "DeleteH", new { id=item.Id })
        </td>
    </tr>
}

</table>
